spring:
  application:
    name: config-server

  profiles:
    active:
    #- native
      - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DanielleSamuels/DistributedSystemsAssignments.git
          searchPaths: chapter8/configserver/src/main/resources/config
          default-label: main
          clone-on-start: true
          force-pull: true
        #native:
          #search-locations: classpath:/config
        #git:
         #   uri: https://github.com/ihuaylupo/config.git
         #   searchPaths: licensingservice

server:
  port: 8071
  
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"